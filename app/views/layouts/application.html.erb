<!DOCTYPE html>
<html>
<head>
  <title>Shpora</title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= csrf_meta_tags %>
</head>
  <body>
    <div class="container">  
      <div class="col-xs-12 col-sm-6 col-md-8">
        <%= render 'layouts/header' %>
        <div class="starter-template">  
          <%= yield %>
        </div>
        <%= render 'layouts/lorem_ipsum' %>
        <%= debug(params) if Rails.env.development? %>
      </div> 
    </div> 
  </body>
</html>
